# Copy values into a file named .env in this directory (gitignored) or export in your shell.
# Prisma reads DATABASE_URL for migrate / generate / studio.

DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/freelance_dev

# Optional: isolated DB for Playwright browser E2E (see docs/testing-playwright.md).
# When set, playwright.config maps this value onto DATABASE_URL for the Next webServer process.
# DATABASE_URL_TEST=postgresql://USER:PASSWORD@localhost:5432/nearwork_playwright
