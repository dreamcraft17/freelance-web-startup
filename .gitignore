node_modules
.turbo
.next
dist
.env
.env.*
coverage
*.log

# Playwright generated auth fixtures (storageState + credentials)
tests/playwright/.auth/

# Playwright run artifacts (local + CI agents)
test-results/
playwright-report/

# Local-only secrets / notes — never commit
credential.md
**/credential.md
